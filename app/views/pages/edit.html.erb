<h1>Pages#edit</h1>
<p>Find me in app/views/pages/edit.html.erb</p>

<%= link_to("<< Back to List", pages_path, :class => 'back-link') %>

<div class="pages edit">
  <h2>Update Page</h2>
  <%#= <%= form_for(@subject, :url => subjects_path(@subject), :method => 'patch') do |f| %>
  <%#= form_for is smart enough to know whether it is a new object or needs to update the object %>
  <%= form_for(@page) do |f| %>

  <%= render(:partial => 'form', :locals => {:f => f})%>

    <div class="form-buttons">
      <%= f.submit("Update Page") %>
    </div>

  <% end %>
</div>
